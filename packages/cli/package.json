{
  "name": "@xuan/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "xuan": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc -p tsconfig.json --noEmit",
    "demo:ziwei": "node dist/demo-ziwei.js"
  },
  "dependencies": {
    "@xuan/agent": "workspace:*",
    "@xuan/core": "workspace:*",
    "@xuan/rag": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
