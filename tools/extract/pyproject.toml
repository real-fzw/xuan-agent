[project]
name = "xuan-extract-worker"
version = "0.1.0"
description = "Local-private document extraction worker for XuanAgent."
requires-python = ">=3.11"
dependencies = [
  "docling>=2.112,<3",
]

[tool.uv]
package = false
